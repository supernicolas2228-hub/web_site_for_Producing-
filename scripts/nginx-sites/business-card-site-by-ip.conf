# По IP :80 → сайт Кирилла (PM2 atlas-next на 127.0.0.1:3030). TrueWeb остаётся на :3000 для доменов.
# Публичный лендинг: не вешайте здесь `allow 1.2.3.4;` + `deny all;` — иначе с VPN/моб. сетей будет 403.
server {
    listen 80;
    server_name 138.124.90.218;

    location / {
        proxy_pass http://127.0.0.1:3030;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
