/**
 * Когда будет готов приём заявок (Sheets, CRM, свой API) — вставь URL и включи отправку в StarterPackModal.
 * Пока null: форма показывает успех локально, данные можно смотреть в админке по событию или в localStorage.
 */
export const STARTER_PACK_SUBMIT_URL: string | null = null;
