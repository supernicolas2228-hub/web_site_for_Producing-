/**
 * Резервные публичные ссылки, если в .env не заданы NEXT_PUBLIC_*.
 * Можно прописать URL здесь и закоммитить — тогда на сервере не обязательны переменные окружения.
 * Оставьте пустую строку, если ссылку задаёте только через .env.
 */
export const publishedUrls = {
  telegram: "https://t.me/+lUgakAg9bUoxZjNi",
  /** Тот же канал — отзывы и материалы; при необходимости замените на отдельную ссылку. */
  telegramReviews: "https://t.me/+lUgakAg9bUoxZjNi",
  payOrJoin: "",
  /** Отдельный лендинг/продукт (не тот же IP, что и этот сайт). Пусто — без внешней кнопки «продукт» */
  productYoutube: "",
  youtube: "",
  instagram: "",
  vk: "",
} as const;
